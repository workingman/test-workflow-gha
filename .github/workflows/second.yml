name: Second Workflow

on:
  pull_request:
    types:
      - closed

jobs:
  use_data:
    runs-on: ubuntu-latest
    if: github.event.pull_request.merged == true

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Cat saved UUID
      run: cat .github/uuid.txt
